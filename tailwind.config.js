module.exports = {
  purge: ['./src/**/*.{js,jsx}'], // if using typescript, add 'ts,tsx' inside the brackets
  darkMode: false, // or 'media' or 'class'
  theme: {
    extend: {},
  },
  variants: {
    extend: {},
  },
  plugins: [],
};
